name: Security & Testing Pipeline [DISABLED - use ci.yml]

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'This workflow is disabled. Use ci.yml instead.'
        required: false

# Disabled: consolidated into ci.yml to reduce CI time from 10+ minutes to ~5-6 minutes
# All jobs (e2e-tests, accessibility-audit) are now part of the main CI pipeline
jobs:
  disabled:
    name: Disabled
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo "disabled"
