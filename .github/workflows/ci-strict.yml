name: CI - Enhanced Pipeline [DISABLED - use ci.yml]

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'This workflow is disabled. Use ci.yml instead.'
        required: false

# Disabled: redundant with ci.yml — see docs/CI_IMPROVEMENT_TASKS.md Task 1.1
jobs:
  disabled:
    name: Disabled
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo "disabled"
